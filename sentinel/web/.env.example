# SENTINEL web console — Arbitrum Sepolia (chain id 421614)
#
# Leave the contract addresses BLANK to run the console in DEMO MODE:
# it streams realistic synthetic agent activity (authorized spends, REVERTED
# attacks, denylist blocks) so the Live Floor is fully demoable pre-deployment.
#
# Fill them in to subscribe to real on-chain events via viem.

NEXT_PUBLIC_ARB_SEPOLIA_RPC=https://sepolia-rollup.arbitrum.io/rpc

# Deployed contract addresses (0x...). Blank => DEMO MODE.
NEXT_PUBLIC_POLICY_REGISTRY=
NEXT_PUBLIC_AGENT_GUARD=
NEXT_PUBLIC_RISK_ENGINE=

# Optional: verified contract links shown on the Proof panel.
NEXT_PUBLIC_RISK_ENGINE_STYLUS=
NEXT_PUBLIC_RISK_ENGINE_SOLIDITY=
